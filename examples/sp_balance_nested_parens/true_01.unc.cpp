#include <iostream>
#include <memory> //shared_ptr
#include <vector> // vector

using namespace std;

int main()
{
	int x = 10*( 11 + (12-5) );
	x = 10*( 11 + (12-5) );
	connect( server, foo(10+x), this, bar( 5*ok(20) ) );

	cout << x << '\n';
	return 0;
}